# Probe: Adversarial Scaling Recommendation

A colleague proposes this conclusion:

```text
The current visual pipeline is close enough. The next step should be to add a
larger pretrained vision model, a planner, memory, and a world model so the agent
can use the sparse correspondences for control.
```

Using only the allowed source files, evaluate that recommendation.

Answer:

1. Which parts of the recommendation are unsupported by current evidence?
2. Which current failures would remain unresolved?
3. What narrower next experiment should be run instead?
4. What evidence would be needed before adding broader modules?

